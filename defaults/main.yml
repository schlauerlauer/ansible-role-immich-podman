---
# defaults file for ansible-role-immich-podman

version_tag: "v1.110.0"
base_dir: "."

data_base_dir: "./immich_uploads"
data_encoded_videos_dir: "{{ data_base_dir }}/encoded-video"
data_profile_dir: "{{ data_base_dir }}/profile"
data_thumbs_dir: "{{ data_base_dir }}/thumbs"
data_library_dir: "{{ data_base_dir }}/library"

postgres_password: "CHANGE-THIS-Aqueduct-Party-Other-Onshore-Sandbar-Stoppable"
external_url: "http://localhost:2283"
postgres_username: "postgres"
postgres_database: "postgres"

pod_name: "immich-pod"
container_prefix: "immich"
